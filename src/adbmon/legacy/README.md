# Original Arduino Uno adbmon (ADBuino lineage)

Preserved for reference. Passive monitor that decoded ADB on AVR PORTD bit 3.

Build (PlatformIO):

```bash
cd legacy
pio run -e uno
```

Superseded by the Pico-native target in the parent directory (`../CMakeLists.txt`).
